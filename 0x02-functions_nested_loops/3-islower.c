#include "main.h"
/**
  * _islower - check if character is lower case
  * @A: the caracter to be checked
  * Return 1 if character is lowercase 0 if uppercase
  */
int _islower(int A)
{
    if (A >= 'a' && A <= 'z')
    
        return (1);
        
    else
    
        return (0);
    
}

